# noisemaker — TouchDesigner port of the Noisemaker shader engine.
# Structural port mirroring ../noisemaker-hlsl (Unity/C#) and ../noisemaker-godot (GDScript).
# The runtime consumes the normalized Render Graph JSON (see ../../docs/GRAPH-JSON-SCHEMA.md)
# and builds a network of GLSL TOP operators. See ../../ARCHITECTURE.md.
__version__ = '0.1.0'
