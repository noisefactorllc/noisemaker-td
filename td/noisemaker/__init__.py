# noisemaker — TouchDesigner port of the Noisemaker shader engine.
# Structural port mirroring the Unity/C# (noisemaker-hlsl) and Godot (noisemaker-godot) ports.
# The runtime consumes the normalized Render Graph JSON (see docs/GRAPH-JSON-SCHEMA.md)
# and builds a network of GLSL TOP operators. See ARCHITECTURE.md at the repo root.
__version__ = '0.1.0'
